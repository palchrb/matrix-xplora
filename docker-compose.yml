services:
  mautrix-xplora:
    image: ghcr.io/palchrb/matrix-xplora:latest
    # Or build locally:
    # build: .
    container_name: mautrix-xplora
    restart: unless-stopped
    volumes:
      - ./data:/data
    environment:
      - UID=1337
      - GID=1337
    # Only needed if your homeserver can't reach the container on the Docker network.
    # ports:
    #   - "29341:29341"
